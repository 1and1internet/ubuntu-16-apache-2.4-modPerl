You can put your own apache config in this directory and it will be invoked the next time you deploy this docker container.

You can also modify any of the existing files in this directory. To revert to the default version of a file, delete it and restart your container.
